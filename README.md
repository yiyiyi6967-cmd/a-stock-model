# A股短线模型 V8.4

在 V8.3 基础上新增“旧版本数据迁移”：
- 支持导入旧版 JSON 状态文件。
- 支持导入 V8.2 / V8.3 SQLite `.db`。
- 迁移采用合并模式，不直接清空当前数据库。
- 候选池、持仓、淘汰记录按股票代码去重。
- 每日评分/胜率 snapshots 合并保留。
- SQLite K线缓存按“股票代码 + 交易日期”合并去重。
- 原来的“完整数据库备份/恢复”继续保留；恢复是整体替换，迁移是合并。
- 行情分析继续使用“完整历史缓存 + 最新增量数据”的完整时间序列。
